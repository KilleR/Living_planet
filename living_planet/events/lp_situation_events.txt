namespace = lp_situation

# Monthly resource check
situation_event = {
	id = lp_situation.1
	hide_window = yes
	is_triggered_only = yes

	immediate = {
		owner = {

		}
		lp_set_growth_resource = yes
	}
}

situation_event = {
	id = lp_situation.101
	hide_window = yes
	is_triggered_only = yes
	immediate = {
		owner = {
			create_message = {
				type = MESSAGE_TYPE_LP_GROWTH
				localization = LP_MESSAGE_SIZE_GAINED
				days = 30
			}
		}
	}
	after = {
		destroy_situation = this
	}
}
